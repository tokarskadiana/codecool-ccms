CREATE TABLE IF NOT EXISTS assignment
(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT,
    date TEXT,
    description TEXT,
    due_date TEXT
    CONSTRAINT chk_due_date CHECK (date IN ('____-__-__')),
    mentor_id INTEGER,
    type TEXT
    CONSTRAINT chk_type CHECK (date IN ('group', 'individual'))
);